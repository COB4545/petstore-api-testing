{
  "id": "${id}",
  "category": {
    "id": "${id1}",
    "name": "${name1}"
  },
  "name": "${name2}",
"photoUrls": ["${photoUrls}"],
  "tags": [
    {
      "id": "${id2}",
      "name": "${name3}"
    }
  ],
  "status": "${status}"
}